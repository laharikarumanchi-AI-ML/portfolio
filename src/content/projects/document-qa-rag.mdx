---
title: "Document Q&A Assistant (RAG)"
oneLiner: "A retrieval-augmented chatbot that answers questions over a set of PDFs, with source citations."
headlineNumber: "Structural abstention — 77 tests across 8 modules"
techStack: ["Python", "sentence-transformers", "FAISS", "Groq / Llama-3.3", "pdfplumber", "Streamlit"]
year: 2026
githubUrl: "https://github.com/laharikarumanchi-AI-ML/document-qa-rag"
demoUrl: "https://huggingface.co/spaces/laharikarumanchi/document-qa-rag"
featured: false
order: 2
category: RAG
---

## Try it

Upload a PDF, ask a question, see an answer with citations to the exact page — or watch it abstain explicitly when the document doesn't cover what you asked.

<iframe
  src="https://laharikarumanchi-document-qa-rag.hf.space"
  title="Document Q&A RAG — live demo"
  width="100%"
  height="700"
  loading="lazy"
  style="border:1px solid var(--rule); border-radius:4px; margin-block:1.5rem; background:#fff;"
></iframe>

The demo above runs on Hugging Face Spaces (free CPU tier — first load may take ~30s as the container wakes up, and the first embedding query downloads the ~80MB sentence-transformers model). [Open in a new tab →](https://huggingface.co/spaces/laharikarumanchi/document-qa-rag)

## Problem

Asking a general-purpose LLM to summarise a stack of PDFs is a recipe for confident-sounding fabrication. The model will gladly answer a question whose answer isn't in the documents at all. The goal was a Q&A assistant where every claim is grounded in a passage you can click through to.

## Approach

Project at a glance:

- PDFs chunked with a recursive character splitter; chunk size tuned for the embedding model's context window.
- Embeddings stored in a local **FAISS** index for fast cosine-similarity retrieval.
- Top-k passages stuffed into the prompt as context, with explicit "answer only from the context" instructions.
- Every response surfaces the source chunk and page number so the user can verify.
- Streamlit UI for upload and chat.

## Results

Side-by-side with a plain LLM prompted on the same PDFs, the RAG version produced noticeably fewer fabricated facts on out-of-document questions — it would say "I can't find that in the provided documents" instead of inventing an answer. Citations made the failure modes inspectable instead of invisible.

## What I'd do differently

I'd add a re-ranker on top of vector search (top-50 → top-5 with a cross-encoder), and split out chunking by document type rather than a single global splitter — academic PDFs and slide decks have very different structure.

## Links

- GitHub: <a href="https://github.com/laharikarumanchi-AI-ML/document-qa-rag">github.com/laharikarumanchi-AI-ML/document-qa-rag</a>
- Live demo: <a href="https://huggingface.co/spaces/laharikarumanchi/document-qa-rag">huggingface.co/spaces/laharikarumanchi/document-qa-rag</a>
